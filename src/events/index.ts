import { ManagerEvents } from "./ManagerEvents";
import { ServerEvents } from "./ServerEvents";

export const Events = {
    Manager: ManagerEvents,
    Server: ServerEvents
}

export {
    ManagerEvents,
    ServerEvents
}